import assert from 'node:assert/strict'; import {examRegistry,liveVisibleExamConfigs,sc200ExamDefinition} from '../src/exams/examRegistry.protected.js'; import {getExamDisplayMetadata} from '../src/exams/examDisplayMetadata.js'; import {getProtectedProfileMetadata} from '../src/exams/protectedProfileMetadata.js';
assert.ok(examRegistry.includes(sc200ExamDefinition));assert.ok(!liveVisibleExamConfigs.includes(sc200ExamDefinition));assert.equal(sc200ExamDefinition.lifecycle,'draft');assert.equal(sc200ExamDefinition.questionBank.length,0);assert.equal(sc200ExamDefinition.profiles.fullMock.totalScoredQuestions,50);assert.equal(sc200ExamDefinition.profiles.fullMock.timeLimitMinutes,100);assert.equal(getProtectedProfileMetadata('sc200').packageVersion,'1.0.0');assert.equal(getExamDisplayMetadata('SC-200').canonicalId,'sc200');console.log(JSON.stringify({ok:true,issue:83,examKey:'sc200',candidateOnly:true,protectedContentRecords:0}));
